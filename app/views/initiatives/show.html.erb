<div class="row">
	<div class="col-md-12">
    <% if permitted_to? :update, :initiatives %>
      <div class="well editInitiative">
  			<p>
          <%= link_to("Editar iniciativa", edit_ong_initiative_path(ong, @initiative), class: "btn btn-xs btn-default pull-right") %><strong>Administrar</strong></p>
  			<hr />
  			<div class="row">
          <%= render 'active_inactive', icon_class: 'pencil', title: 'Firmas', object: @initiative, attribute: :signs_active, url: [ong, @initiative], event_name: 'form:signs-active' %>

          <%= render layout: 'partials/modal_window', locals: { modal_id: "signs-activation-confirmation", title: "Firmas para tu iniciativa", ok_text: "Activar firmas", cancel_text: "Cancelar" } do %>
          <% content_for :modal_footer do %>
          <button type="button" class="btn btn-link" data-dismiss="modal">Jojo</button>
          <button type="button" class="btn btn-default">Jaja</button>
          <% end %>
          <strong>¿Estás seguro de que quieres activar la recopilación de firmas para tu iniciativa?</strong>
          <p>Esto creará un botón "Firmar" en tu iniciativa, instando a los visitantes a que se adhieran a ella.</p>
          <p>Una vez que tengas adherentes podrás ver y descargar la lista de datos de los mismos.</p>
          <% end %>
          
          
          <%= render 'active_inactive', icon_class: 'money', title: 'Donaciones', object: @initiative, attribute: :donations_active, url: [ong, @initiative] %>
          <%= render 'active_inactive', icon_class: 'group', title: 'Manifestación', object: @initiative, attribute: :protest_active, url: [ong, @initiative] %>
          <%= render 'active_inactive', icon_class: 'comments', title: 'Spamear', object: @initiative, attribute: :spam_active, url: [ong, @initiative] %>
  			</div>
  		</div>
    <% end %>
    
		<h1><%= @initiative.title %>
      <%= link_to facebook_url(@initiative), target: "_blank" do %>
        <i class="icon-facebook-sign"></i>
      <% end %>
      <%= link_to tweet_url(@initiative), target: "_blank" do %>
        <i class="icon-twitter-sign"></i>
      <% end %>
    </h1>
	</div>
</div>

<div class="row">
	<div class="col-xs-12 col-sm-6 col-md-8 col-lg-8">
    <% if @initiative.signs_active %>
  		<div id="signButton" class="well clearfix actionBox">
  			<a id="goto-sign-button" href="#signInitiative" class="pull-right btn btn-info">Firma</a>
  			<i class="pull-left icon-pencil"></i>
  			<p>Contigo podemos ser <strong data-signs-count="1"><%= @initiative.signs_count + 1 %></strong> adherentes</p>
  		</div>
    <% end %>
    <% if @initiative.donations_active %>
  		<div id="donateButton" class="well clearfix actionBox">
  			<a href="#" class="pull-right btn btn-warning">Dona</a>
  			<i class="pull-left icon-bullhorn"></i>
  			<p>Con tu ayuda podríamos llegar a <%= @initiative.signs_count + 2000 %> personas</p>
  		</div>
    <% end %>
		<div class="textBox">
			<%= @initiative.description.html_safe %>
		</div>
		<%#= donation_button %>
		<div class="well statistics">
			<h6><i class="icon-bar-chart"></i>Estadísticas de esta iniciativa</h6>
			<hr>
			<div class="row">
				<div class="col-xs-12 col-sm-6 col-md-4 col-lg-4"><i class="icon-pencil"></i><strong data-signs-count><%= @initiative.signs_count || 0 %></strong> firmas</div>
				<div class="col-xs-12 col-sm-6 col-md-4 col-lg-4"><i class="icon-money"></i><strong><%= @initiative.donations_count || 0 %></strong> donaciones</div>
				<div class="col-xs-12 col-sm-6 col-md-4 col-lg-4"><i class="icon-eye-open"></i><strong>2000</strong> visitas</div>
			</div>
		</div>
		<% if @initiative.signs_active %>
      <div id="signInitiative" class="well">
        <%= render "signs/form", sign: Sign.new %>
      </div>
		<% end %>
	</div>

	<div class="col-xs-12 col-sm-6 col-md-4 col-lg-4">
		<div class="well">
			<h6>#<%= @initiative.hashtag %></h6>
		</div>
		<div class="textBox">
			<h6><i class="icon-external-link-sign"></i>Iniciativas relacionadas</h6>
			<div class="list-group">
        <% @initiatives.each do |i| %>
          <%= link_to i.title, ong_initiative_path(ong, i), class: "list-group-item" %>
        <% end %>
			</div>
		</div>
	</div>

</div>