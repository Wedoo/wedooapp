<h1>Crea tu iniciativa</h1>

<%= simple_form_for(@initiative, url: ong_initiatives_path(ong), html: { class: 'form-horizontal' }) do |f| %>

	<%= f.input :title %>
	<%= f.input :description %>
	<%= f.input :hashtag %>
	<%= f.submit "Crear" %>
<% end %>